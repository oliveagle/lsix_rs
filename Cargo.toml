[package]
name = "lsix"
version = "0.1.0"
edition = "2021"
description = "Like ls, but for images - shows thumbnails in terminal using SIXEL"
license = "GPL-3.0+"

[dependencies]
anyhow = "1.0"
glob = "0.3"
regex = "1.10"
clap = { version = "4.5", features = ["derive"] }
rayon = "1.10"
